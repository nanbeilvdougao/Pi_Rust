# Parity Ledger

The parity ledger records behavior against the TypeScript Pi baseline and existing Rust implementations (`pi_agent_rust`, `pi-rs`).

Status legend:

- `must-match` — behavior should stay compatible with TS pi.
- `intentional-difference` — behavior differs because the redesign improves
  local, Chinese, security, or maintenance goals.
- `must-match-shape` — wire/format shape matches but the surface is smaller or
  larger than TS pi on purpose.
- `future` — explicitly out of MVP scope.

| Area | Status | Decision | Evidence |
| --- | --- | --- | --- |
| CLI help/version | must-match | clap-based parser with both `pi --help` and legacy `pi doctor` / `pi sessions` subcommands | `crates/pi-cli` |
| CLI doctor diagnostics | intentional-difference | `pi doctor` reports cargo, rustc, curl, session path, all provider credential envs, rust version, pi version | `crates/pi-cli` |
| CLI model listing | must-match | `--list-models` lists provider model presets including defaults marker | `crates/pi-cli`, `crates/pi-providers` |
| CLI tool listing and selection | must-match | `--list-tools` and `--tools` expose JSON-Schema parameters | `crates/pi-cli`, `crates/pi-tools` |
| CLI permission mode | intentional-difference | `--permission read-only|confirm|trusted|plan` selects the engine mode at startup | `crates/pi-cli`, `crates/pi-permissions` |
| CLI locale | intentional-difference | `--locale zh-cn|en` switches the default system prompt language | `crates/pi-cli`, `crates/pi-agent` |
| CLI interactive TUI | must-match-shape | `pi` (no prompt) or `-i` launches a ratatui+crossterm TUI with streaming, history, Ctrl+C interrupt, Ctrl+L clear, Ctrl+J multi-line | `crates/pi-tui`, `crates/pi-cli` |
| CLI RPC mode | must-match-shape | `pi --rpc` runs a JSON-RPC stdio loop: `health`, `list_providers`, `list_tools`, `complete`, `shutdown` | `crates/pi-cli/src/rpc.rs` |
| Persistent settings | must-match-shape | TOML layered settings: `~/.pi-rust/config.toml` (user) then `<workspace>/.pi/config.toml` (workspace); CLI flags win | `crates/pi-agent/src/settings.rs` |
| Skills system | must-match-shape | `<workspace>/.pi/skills/*.md` with optional TOML frontmatter; `always` skills inject into the system prompt; `keyword`/`command` triggers exposed | `crates/pi-agent/src/skills.rs` |
| Custom slash commands | must-match-shape | `<workspace>/.pi/commands/<name>.md` registers `/<name>` with `{{argument}}` substitution | `crates/pi-agent/src/slash.rs` |
| Provider registry | intentional-difference | Chinese providers (Moonshot, DeepSeek, Qwen/DashScope, Zhipu GLM, MiniMax) are first-class alongside OpenAI, Anthropic, Gemini, Ollama, and echo | `crates/pi-providers` |
| Ollama provider execution | must-match | Real `/api/chat` execution path with NDJSON streaming, structured tool calls, usage tokens | `crates/pi-providers/src/ollama.rs` |
| OpenAI-compatible providers | must-match-shape | Real `chat/completions` (non-streaming) and SSE streaming paths, with `tool_calls`, `reasoning_content` (DeepSeek), `tool_choice=auto`, `stream_options.include_usage` | `crates/pi-providers/src/openai.rs` |
| Anthropic provider | must-match-shape | Real `v1/messages` API, SSE streaming, tool_use blocks, thinking_delta, prompt caching usage fields | `crates/pi-providers/src/anthropic.rs` |
| Gemini provider | must-match-shape | Real `v1beta/models/:generateContent` and `:streamGenerateContent?alt=sse`, function calling, usage metadata | `crates/pi-providers/src/gemini.rs` |
| Provider stream event model | must-match-shape | Unified `StreamEvent::{MessageStart,TextDelta,ThinkingDelta,ToolCallDelta,UsageDelta,MessageDone}` for every provider | `crates/pi-core`, `crates/pi-providers`, `crates/pi-agent` |
| Provider-driven tool calls | must-match | Multi-round tool loop: assistant tool-call turn is persisted before the tool result, matching OpenAI/Anthropic replay requirements | `crates/pi-agent` |
| System prompt | must-match-shape | Default prompt assembled from locale, cwd, os, arch, date, provider/model, the live tool inventory, plus always-on skills | `crates/pi-agent/src/system_prompt.rs`, `crates/pi-agent/src/skills.rs` |
| Context compaction | must-match-shape | Triggers when estimated tokens exceed `context_window_tokens * compaction_threshold`; keeps head (system + first user) and last N user/assistant pairs intact, summarizes the middle through the same provider | `crates/pi-agent/src/compaction.rs` |
| Slash commands (built-in) | must-match | `/help`, `/clear`, `/sessions`, `/model`, `/tools`, `/compact`, `/permission`, `/quit` handled in-agent before provider routing | `crates/pi-agent/src/slash.rs` |
| Session format and discovery | must-match-shape | Append-only JSONL with serde-derived schema, sanitized session ids, `--continue`, `--resume <ID>`, `--session <ID>`, `--list-sessions` (showing last user excerpt), `--delete-session`, `--rename-session`, `--export-session`, `--export-json` | `crates/pi-session`, `crates/pi-cli` |
| Tool permissions | intentional-difference | Mutating tools, network, and extension hostcalls all gate through `pi-permissions::PermissionEngine`, with dangerous-target blocklist and audit log | `crates/pi-permissions`, `crates/pi-tools` |
| Built-in tools surface | must-match-shape | `read` (with offset/limit/line_numbers), `write` (with create_dirs), `edit` (unique-match + diff preview + replace_all), `bash` (cwd, timeout_ms, output truncation), `search`, `grep` (regex, case_sensitive), `find` (glob), `ls`, `epkg` | `crates/pi-tools` |
| Tool input dual mode | intentional-difference | Each tool accepts both structured JSON `parameters` (from provider tool calls) and a legacy plain-string form (for `/tool` shortcuts) | `crates/pi-tools` |
| Output truncation | must-match-shape | Bash and other unbounded tools cap output by both bytes and lines and emit a `... <n> lines / <m> bytes elided ...` separator | `crates/pi-tools/src/truncate.rs` |
| Multimodal attachments | must-match-shape | `Message::attachments` carries base64 or URL image attachments. Mapped to OpenAI `image_url`, Anthropic `image`, Gemini `inlineData`/`fileData`. Ctrl+V in the TUI pastes images straight from the clipboard into the next turn | `crates/pi-core`, `crates/pi-providers`, `crates/pi-tui/src/clipboard.rs` |
| Model alias resolution | must-match | `pi --model sonnet` (or any provider/model literal) resolves through `pi_providers::resolve_alias`; `pi --list-aliases` prints the table | `crates/pi-providers/src/aliases.rs`, `crates/pi-cli/src/main.rs` |
| TUI autocomplete + `@` file references | must-match-shape | Tab opens a completion popup for slash commands or fuzzy file paths; `@path` expands inline into the prompt with file contents attached | `crates/pi-tui/src/completion.rs` |
| Clipboard image paste | must-match-shape | Ctrl+V reads the system clipboard via `arboard`, encodes images as inline PNG (zero-deps hand-rolled writer), attaches them to the next message | `crates/pi-tui/src/clipboard.rs` |
| Configurable keybindings | must-match-shape | All TUI actions go through `KeyBindings`; `~/.pi-rust/keybindings.toml` and `<workspace>/.pi/keybindings.toml` layer overrides; chord parser supports Ctrl/Alt/Shift/Super | `crates/pi-tui/src/keybindings.rs` |
| Encrypted credential storage | must-match-shape | `pi auth set/list/remove` writes ChaCha20-Poly1305 encrypted TOML under `~/.pi-rust/auth.enc`; optional `keyring` feature plugs in macOS Keychain / Linux Secret Service / Windows Credential Manager. Process env still wins over disk | `crates/pi-auth` |
| Session-level cwd | must-match-shape | First line of a `.jsonl` session is a `SessionHeader { type: "session", version: 2, cwd, created_ms, … }`; `--resume` chdir's into the recorded cwd if it still exists | `crates/pi-session` |
| TUI theme system | must-match-shape | `dark` / `light` / `solarized` built-ins; `~/.pi-rust/theme.toml` and workspace overrides; `--theme` flag chooses base; named ANSI and `#rrggbb` colors | `crates/pi-tui/src/theme.rs` |
| HTML session export | must-match | `pi --export-html <id>` produces a single self-contained HTML page (inline CSS, folded tool outputs, no external CDN) | `crates/pi-session/src/export_html.rs` |
| Localized error hints | must-match-shape | `pi_core::hint_for` maps every `PiErrorKind` (plus message keywords) to zh-CN / en summaries with bullet-pointed next steps; CLI appends them to stderr | `crates/pi-core/src/error_hints.rs` |
| Provider health probe | must-match-shape | `pi --probe` (alone or with `pi doctor`) issues a cheap credentialed read against every configured provider and reports `ok` / `auth_fail` / `unreachable` / `missing_credential` / `unsupported` | `crates/pi-providers/src/probe.rs` |
| Agent harness / faux provider | must-match-shape | `pi_providers::FauxProvider` plus `register_test_provider` give the agent loop a deterministic stand-in with scripted turns and request recording; used by the agent test suite | `crates/pi-providers/src/faux.rs`, `crates/pi-agent/tests/faux_harness.rs` |
| MCP server integration | must-match-shape | `.pi/mcp.toml` declares servers; `pi_mcp::McpManager` spawns them, drives JSON-RPC 2.0 over stdio (`initialize` + `tools/list` + `tools/call`), and registers every remote tool into `pi-tools::ToolRuntime` as a first-class capability-gated tool | `crates/pi-mcp` |
| WASM extension full-duplex RPC | must-match-shape | Custom WASI `BlockingStdin` lets the guest read host replies between writes; guest runs on a worker thread while the host streams stdout, dispatches hostcalls, and feeds replies back without quitting the module | `crates/pi-ext/src/wasm.rs` |
| Comparative benches (session / tool / agent) | intentional-difference | New `pi-bench` benches for session_save (2.18 ms / 100 msgs), tool_dispatch (9.6 µs read), and agent_loop (177 µs faux round-trip), captured under `docs/evidence/comparative-bench.md` | `crates/pi-bench/benches/{session_save,tool_dispatch,agent_loop}.rs` |
| Extension runtime | must-match-shape | Process-backed JSON-RPC host (`pi-ext::process::ExtensionHost`) with capability-gated hostcalls. WASM host is future work but the ABI is stable | `crates/pi-ext` |
| Streaming cancellation | intentional-difference | Cooperative `Arc<AtomicBool>` cancel handle; TUI Ctrl+C flips it; provider stream sinks check `cancelled()` between events | `crates/pi-core`, `crates/pi-agent`, `crates/pi-tui` |
| Benchmark harness | intentional-difference | `pi-bench` crate exposes criterion benches for session append, JSON serialize, token estimate, and the comparable workloads (SSE parse, output truncation) | `crates/pi-bench` |
| Comparative benchmarks | intentional-difference | Three-way side-by-side numbers vs pi_agent_rust and pi-rs captured under `docs/evidence/comparative-bench.md`. Pi Rust 4.8–5.0× faster on SSE parsing, 2.1–3.7× faster on truncation, and lowest cold-start wall time (0.44 s vs 0.57 s pi-rs vs 0.76 s pi_agent_rust). `pi-bench` adds `session_save` / `tool_dispatch` / `agent_loop` as new comparable axes | `docs/evidence/comparative-bench.md`, `crates/pi-bench/benches/{session_save,tool_dispatch,agent_loop,comparable}.rs` |
| `multi_edit` tool | must-match | Atomic multi-replace: all `edits[i].old` must exist before any byte hits disk; integrated with the file-mutation queue so concurrent writers serialize | `crates/pi-tools/src/lib.rs::MultiEditTool`, regression test `ts_pi_regressions::ts2835_tools_allowlist_filters` |
| File mutation queue | must-match-shape | Per-path `Mutex` registry plus snapshot/commit guard detects external modifications between read and write, mirroring TS pi's `file-mutation-queue.ts` | `crates/pi-tools/src/mutation_queue.rs` |
| Anthropic OAuth login | must-match-shape | `pi auth login <provider>` runs the full PKCE flow with a loopback callback server; tokens (access + refresh) land in the encrypted `pi-auth` store. `PI_OAUTH_CLIENT_ID_<PROVIDER>` selects the registered client | `crates/pi-auth/src/oauth.rs`, `crates/pi-cli/src/auth.rs::login` |
| AWS Bedrock provider | must-match-shape | `pi --provider bedrock` ships a hand-rolled SigV4 signer (RFC 4231 test-vector verified) and routes Anthropic-on-Bedrock through `invokeModel`. Streaming via AWS event-stream binary framing remains future work but the non-streaming path is complete | `crates/pi-providers/src/sigv4.rs`, `crates/pi-providers/src/bedrock.rs` |
| Image-generation provider | must-match-shape | `image_generate` tool calls OpenAI `images/generations` (default) or Imagen `:predict`; PNG bytes are saved to a chosen path under capability gates (Network + WriteFile) | `crates/pi-tools/src/image_gen.rs` |
| Interactive session picker | must-match | `pi sessions` (no args) and `pi --pick` open a ratatui list of sessions sorted newest-first; Enter selects, `n` opens a new session, Esc cancels | `crates/pi-tui/src/session_picker.rs` |
| First-run config wizard | must-match-shape | When `~/.pi-rust/config.toml` does not exist, the CLI walks the user through provider → model → key entry on a TTY; on non-TTY it short-circuits | `crates/pi-tui/src/config_selector.rs` |
| Package-manager detection | must-match-shape | `pi-agent::source_info` detects cargo / npm / pnpm / yarn / bun / pip / poetry / uv / go / maven / gradle; results are injected into the system prompt | `crates/pi-agent/src/source_info.rs` |
| TUI footer data provider | must-match-shape | Status block renders git branch+commit+dirty, provider/model, token usage progress bar relative to context window, and last-error class | `crates/pi-tui/src/footer.rs` |
| `source-info` system prompt | must-match | git branch / short commit / remote URL / dirty flag and the detected project managers land in the system prompt's `Source info:` section | `crates/pi-agent/src/source_info.rs` |
| `--trace` chrome-tracing | must-match-shape | `pi_core::timings::span()` instrument the agent loop's provider / tool / compaction critical sections; `pi --trace path.json` emits a chrome://tracing-compatible event array | `crates/pi-core/src/timings.rs` |
| Opt-in anonymous telemetry | must-match | Defaults off, `PI_TELEMETRY=1` opts in, `DO_NOT_TRACK=1` always wins. Events carry version + command + error class + bucketed duration only; install id is a per-machine UUID v4 | `crates/pi-core/src/telemetry.rs` |
| TS pi `.pi/` directory interop | must-match | JSONL header v3 (camelCase, `timestamp` string, `parentSession`) parses transparently; `.pi/config.json` is read alongside `.pi/config.toml`; `.pi/skills` / `.pi/commands` markdown files are already shared | `crates/pi-session/src/interop.rs`, `crates/pi-agent/src/settings.rs` |
| Extension wrapper | must-match-shape | `pi_ext::ToolBridge` implements `HostcallResolver` to dispatch `Hostcall::Tool` into a `ToolRuntime` (including registered MCP tools) under the permission engine, mirroring TS `extensions/wrapper.ts` | `crates/pi-ext/src/wrapper.rs` |
| Cross-platform release CI | must-match-shape | `.github/workflows/{ci,release}.yml` runs cargo test on Linux/macOS/Windows, plus a feature-matrix job; release tags build five binaries (`pi-{x86_64,aarch64}-{linux,macos,windows}`) matching the asset name `pi --self-update` expects | `.github/workflows/release.yml` |
| TS-pi regression suite | must-match-shape | 20-case Rust integration suite under `crates/pi-agent/tests/ts_pi_regressions.rs` translates upstream issue regressions (ts-2023, 2753, 2781, 2835, 2860, 3217, 3302, 3317, 3592, 3686, 3688, 3982, 4167) plus 7 pi-rust-local invariants | `crates/pi-agent/tests/ts_pi_regressions.rs` |
| Azure OpenAI provider | must-match-shape | `pi --provider azure` with deployment-path URL, `api-version` query, `api-key` or AAD bearer auth | `crates/pi-providers/src/azure.rs` |
| Google Vertex AI provider | must-match-shape | `pi --provider vertex` against `v1/projects/{p}/locations/{r}/publishers/google/models/{m}` with `VERTEX_ACCESS_TOKEN`; reuses Gemini body/parser | `crates/pi-providers/src/vertex.rs` |
| GitHub Copilot provider | must-match-shape | `pi --provider copilot` with Editor-Version / Copilot-Integration-Id headers + `GITHUB_COPILOT_TOKEN` | `crates/pi-providers/src/copilot.rs` |
| OpenRouter provider | must-match | OpenAI-compat against `openrouter.ai/api/v1`, vendor/model aliases supported | `crates/pi-providers/src/openai.rs::OpenAiCompatibleProvider::openrouter` |
| Mistral provider | must-match | OpenAI-compat against `api.mistral.ai/v1` | `crates/pi-providers/src/openai.rs::OpenAiCompatibleProvider::mistral` |
| Cloudflare Workers AI provider | must-match-shape | `accounts/{id}/ai/run/{model}` with `CLOUDFLARE_API_TOKEN` bearer | `crates/pi-providers/src/cloudflare.rs` |
| OpenAI Responses API provider | must-match-shape | Real `/v1/responses` client with typed SSE events (`response.output_text.delta`, `response.completed`) and multimodal `input_image` blocks | `crates/pi-providers/src/openai_responses.rs` |
| `pi --file` / `--message-file` | must-match | Append file contents to the first user message (multi-file supported) or replace prompt outright | `crates/pi-cli/src/main.rs` |
| fs-watch skill/command hot reload | must-match-shape | `WorkspaceWatcher` polls `.pi/{skills,commands}` mtimes on a 1 s tick; agent swaps in fresh `SkillSet` / `SlashRegistry` on the next turn | `crates/pi-agent/src/fs_watch.rs` |
| OAuth-specific auth-guidance | must-match-shape | `pi_core::auth_guidance_for` returns per-provider recovery steps; CLI emits them on 401/403/缺少凭证/expired | `crates/pi-core/src/auth_guidance.rs` |
| CHANGELOG.md | must-match | keep-a-changelog format backfilled from git log | `CHANGELOG.md` |
| TUI inline image | must-match-shape | iTerm2 / WezTerm / Kitty inline image protocol detection; text fallback when none available | `crates/pi-tui/src/terminal_image.rs` |
| Multi-line EditorComponent | must-match-shape | Logical-line editor with word-skip, home/end, undo/redo, kill-ring (yank + rotate) | `crates/pi-tui/src/editor.rs` |
| Clipboard image scaling | must-match-shape | Nearest-neighbor downsample to 1568 px before PNG encoding; EXIF orientation is baked-in by arboard | `crates/pi-tui/src/clipboard.rs::scale_to_max` |
| Capability sandbox runtime | must-match-shape | `pi_permissions::apply_sandbox` wraps Command with bwrap / firejail (Linux), sandbox-exec (macOS); Windows uses permission engine + job-object placeholder | `crates/pi-permissions/src/sandbox.rs` |
| WASM extension host | intentional-difference | `pi-ext` ships a wasmtime + wasi-preview-1 host behind feature `wasm`. Same JSON-RPC protocol as the process host; capability gates apply | `crates/pi-ext/src/wasm.rs` |
| SQLite session index | intentional-difference | Optional `sqlite-index` feature mirrors JSONL into a SQLite database with FTS5 full-text search; JSONL stays canonical | `crates/pi-session/src/sqlite_index.rs` |
| Self-update | must-match-shape | `pi --version-check` queries GitHub Releases; `pi --self-update --yes` atomically swaps the running binary with the matching release asset | `crates/pi-cli/src/update.rs` |
| Assistant markdown rendering | must-match-shape | `pulldown-cmark` parses each assistant reply; ratatui spans render headings, lists, fenced code blocks (with language tag), inline code, blockquotes, links, strikethrough | `crates/pi-tui/src/markdown.rs` |
| In-TUI model selector | must-match-shape | Ctrl+M overlay lists every provider/model pair from `ProviderRegistry::builtin()` and swaps `AppConfig.model` live | `crates/pi-tui/src/overlay.rs::Overlay::Model` |
| In-TUI theme selector | must-match-shape | Ctrl+T overlay switches between dark / light / solarized at runtime without restarting | `crates/pi-tui/src/overlay.rs::Overlay::Theme` |
| In-TUI login dialog | must-match-shape | Ctrl+G overlay walks provider → OAuth-or-key → API-key input; pushes credentials into `EncryptedFileStore` (PKCE OAuth runs through `pi-auth::oauth`) | `crates/pi-tui/src/overlay.rs::Overlay::Login` |
| In-TUI settings selector | must-match-shape | F3 overlay toggles stream / tools_enabled / print_mode / permission_mode / compaction_threshold in place; values cycle on Enter | `crates/pi-tui/src/overlay.rs::Overlay::Settings` |
| Thinking-budget selector | must-match-shape | F2 overlay sets `AppConfig.thinking_level` (none/low/medium/high); persisted in `pi-core::ThinkingLevel` | `crates/pi-tui/src/overlay.rs::Overlay::Thinking`, `crates/pi-core/src/lib.rs::ThinkingLevel` |
| Workspace tree selector | must-match-shape | Ctrl+P opens a depth-3 file tree (skip `.git`/`target`/`node_modules`); Enter inserts `@<relpath>` into the input | `crates/pi-tui/src/overlay.rs::Overlay::Tree` |
| Image attachments overlay | must-match-shape | Alt+I lists pending image attachments by index/MIME/size; Enter removes the highlighted entry from the next turn | `crates/pi-tui/src/overlay.rs::Overlay::ShowImages` |
| Extension overlay | must-match-shape | Ctrl+E lists `.pi/extensions/*` and toggles them; empty state hints at the install path | `crates/pi-tui/src/overlay.rs::Overlay::Extension` |
| Tool-execution streaming widget | must-match-shape | New `Event::ToolProgress { name, line }` channel; TUI shows a live last-6-lines popup over the transcript while the tool runs; bash/shell get a `$` title prefix as a dedicated bash widget | `crates/pi-core/src/lib.rs::Event::ToolProgress`, `crates/pi-tui/src/lib.rs::draw` |
| Compaction summary widget | must-match-shape | After `Event::Compacted`, the transcript inserts a multi-line system entry with a 20-cell █/░ progress bar and the saved-token percentage | `crates/pi-tui/src/lib.rs::apply_stream_event` |
| OpenAI Codex Responses provider | must-match-shape | `pi --provider openai-codex-responses` routes to the codex tenant (`/codex/v1/responses`), honoring `OPENAI_CODEX_API_KEY` and falling back to `OPENAI_API_KEY`; body shape delegates to `openai_responses` so the two cannot drift | `crates/pi-providers/src/openai_codex_responses.rs` |
| OpenRouter image generation route | must-match-shape | `image_generate` accepts `image_provider="openrouter"`; calls `openrouter.ai/api/v1/images/generations` with `OPENROUTER_API_KEY` and honors b64 or URL response shapes | `crates/pi-tools/src/image_gen.rs::call_openrouter` |
| pi-rpc-client crate | must-match-shape | New `pi-rpc-client` crate spawns `pi --rpc-stdio` as a subprocess and exposes `health` / `list_providers` / `list_tools` / `complete` / `shutdown`; mirrors the TS SDK shape | `crates/pi-rpc-client/src/lib.rs` |
| Branch summarizer (parallel) | must-match-shape | `BranchSummarizer::summarize` runs each branch through the provider on its own `std::thread` (bounded by `max_concurrency`), preserves input order on output, and offers `merge_summaries` for system-prompt injection | `crates/pi-agent/src/branch_summary.rs` |
| Fine-grained capabilities + landlock | must-match-shape | `Capability` adds `DeleteFile` / `ChangeMode` / `BindSocket`; `pi-permissions::restrict_self` applies a Landlock ruleset derived from `SandboxProfile + Capability[]` on Linux (≥ 5.13); macOS/Windows fall back to `apply_sandbox` | `crates/pi-permissions/src/landlock_apply.rs`, `crates/pi-permissions/src/lib.rs` |
| Bedrock streaming | must-match-shape | `invokeModelWithResponseStream` now produces real per-token deltas: hand-rolled AWS `application/vnd.amazon.eventstream` parser with prelude+message CRC32, header decoding, embedded base64 Anthropic event mapping (text_delta / thinking_delta / input_json_delta / message_delta usage) | `crates/pi-providers/src/aws_event_stream.rs`, `crates/pi-providers/src/bedrock.rs::stream` |
| `webfetch` tool | must-match-shape | HTTPS GET behind `Capability::Network`, size + timeout caps, HTML→markdown converter strips scripts/styles/iframes and keeps headings/lists/inline anchors; binary payloads land as a metadata-only placeholder | `crates/pi-tools/src/webfetch.rs` |
| `websearch` tool | must-match-shape | Three backends (`tavily` / `brave` / `serpapi`) selectable per call or via `WEB_SEARCH_PROVIDER`; each pulls `<key>` from env or `pi-auth`; results normalize to `{title, url, snippet}` rows | `crates/pi-tools/src/websearch.rs` |
| `todo` tool + session persistence | must-match-shape | `add` / `update` / `list` / `clear` actions store under `.pi/todos/<session_id>.json` keyed by `PI_SESSION_ID`; status enum `pending` / `in_progress` / `completed`; TUI renders the top-right todo panel from the same file | `crates/pi-tools/src/todo.rs` |
| `task` tool (subagent spawn) | must-match-shape | Parent agent delegates to a fresh `AgentRuntime` with `InMemorySessionStore`, narrowed allowlist (drops `task` to prevent recursion), bounded `max_steps`, optional system override; child's final assistant message is returned | `crates/pi-tools/src/task.rs`, `crates/pi-agent/src/subagent.rs`, `crates/pi-session/src/lib.rs::InMemorySessionStore` |
| Agent + MCP selectors (TUI) | must-match-shape | Ctrl+A overlay lists `.pi/agents/*` toml files and swaps the parent's system prompt to the selected agent; Ctrl+S overlay reads `.pi/mcp.toml` and shows server status | `crates/pi-tui/src/overlay.rs::Overlay::Agent`, `crates/pi-tui/src/overlay.rs::Overlay::Mcp` |
| Todo TUI panel | must-match-shape | Top-right floating box during interactive mode reads `.pi/todos/<session_id>.json` and renders `[ ]` / `[…]` / `[x]` glyphs themed by status | `crates/pi-tui/src/lib.rs::draw` |
| MCP resources + prompts + notifications | must-match-shape | `McpServer` now negotiates capabilities at `initialize`, fetches `resources/list` + `prompts/list` if advertised, exposes `resources/read` / `prompts/get`, and `drain_notifications` collects async server-pushed `notifications/*` frames | `crates/pi-mcp/src/lib.rs` |
| Extension host resources / prompts / notify | must-match-shape | New `Hostcall::ResourceList` / `ResourceRead` / `PromptList` / `PromptGet` / `NotifyEvent`; `ToolBridge` carries a resource + prompt catalogue (built from `.pi/resources/*` + `.pi/prompts/*.md` or MCP fan-in) and an event sink the agent forwards into the streaming widget | `crates/pi-ext/src/lib.rs::Hostcall`, `crates/pi-ext/src/wrapper.rs` |
| MCP server-initiated sampling | must-match-shape | `McpServer::set_sampling_handler` registers a `SamplingHandler`; when the server pushes a `sampling/createMessage` request mid-call, the rpc loop dispatches it to the handler and writes the host's `{role,content}` reply back, so a remote MCP server can reuse Pi's model | `crates/pi-mcp/src/lib.rs::SamplingHandler`, `rpc_request_full` |
| MCP `$/cancelRequest` + cancel flag | must-match-shape | `McpServer::cancel_in_flight(id)` sends the JSON-RPC `$/cancelRequest` notification and flips a local `AtomicBool` so the in-flight read returns `PiErrorKind::Cancelled` instead of waiting for the server | `crates/pi-mcp/src/lib.rs::McpServer::cancel_in_flight` |
| MCP `notifications/progress` dispatch | must-match-shape | `McpServer::set_progress_handler` lets the agent forward server-pushed progress frames into `Event::ToolProgress`, so MCP-driven long tasks appear in the TUI streaming widget identically to built-in tools | `crates/pi-mcp/src/lib.rs::ProgressHandler` |
| Workspace hooks lifecycle | must-match-shape | `<workspace>/.pi/hooks/{pre-turn,pre-tool,post-tool,post-turn}.sh` run with `PI_PHASE` / `PI_PROMPT` / `PI_TOOL_NAME` / `PI_TOOL_INPUT` / `PI_TOOL_OUTPUT` / `PI_SESSION_ID` env vars; pre-* exit-non-zero blocks the call, post-* is advisory; missing scripts are a no-op | `crates/pi-agent/src/hooks.rs`, `crates/pi-agent/src/lib.rs` |
